machine:
  ruby:
    version: 2.3.0

  environment:
    LOGIDZE_DB_NAME: circle_test
    LOGIDZE_DB_USER: ubuntu

dependencies:
  pre:
    - gem install bundler -v 1.11.2

database:
  override:
    - bundle exec rake dummy:db:test:prepare
